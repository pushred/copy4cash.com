<div>
  <slot />
</div>

<style>
  div {
    margin: 3px 3px var(--space-bottom) 3px;
    padding: var(--space-5);
    border: 1px solid var(--purple);
    border-radius: var(--space-5);
    box-shadow: 0 0 2px var(--purple), 0 0 3px var(--purple);
  }
</style>
